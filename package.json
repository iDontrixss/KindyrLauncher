{
  "name": "loryq-launcher-site",
  "version": "1.1.0",
  "private": true,
  "description": "Loryq Launcher official website (static landing page).",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
